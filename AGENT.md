# Project Agent Notes

After each minor or major change to the codebase, only with the user's approval, copy the modified files (or the entire project) to `/home/apoapostolov/git-public/Old-School-Essentials-Character-Creator/` and then commit the changes in that public repository.
